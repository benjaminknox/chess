import * as React from 'react'
import { NewForm } from './NewForm'
import { mount } from '@cypress/react'

describe('NewForm', () => {
  it('should show a button for getting a new game', () => {})
})
